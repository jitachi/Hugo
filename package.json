{
  "name": "website",
  "version": "1.0.0",
  "description": "Hugo sandbox",
  "main": "index.js",
  "dependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-go-template": "^0.0.15",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "cd themes/blank && npm install && npm run build",
    "build": "npm run build:css",
    "start": "hugo server --bind 0.0.0.0 --port $PORT --environment production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
